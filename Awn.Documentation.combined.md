# Оглавление

1. [02 Components](#1-02-components)
   1.1. [Index](#11-index)
   1.2. [02 Nodes-types](#12-02-nodes-types)
      1.2.1. [Index](#121-index)
      1.2.2. [01 Folders](#122-01-folders)
         1.2.2.1. [00 Index](#1221-00-index)
         1.2.2.2. [01 Workspace-vault](#1222-01-workspace-vault)
         1.2.2.3. [02 Space](#1223-02-space)
         1.2.2.4. [03 Cluster](#1224-03-cluster)
         1.2.2.5. [04 Book](#1225-04-book)
         1.2.2.6. [05 Collection](#1226-05-collection)
         1.2.2.7. [06 Intro ИЛИ REPO](#1227-06-intro-ili-repo)
   1.3. [03 Memories-types](#13-03-memories-types)
      1.3.1. [00 Index](#131-00-index)
      1.3.2. [Content](#132-content)
         1.3.2.1. [Inner](#1321-inner)
         1.3.2.2. [Outer](#1322-outer)
      1.3.3. [Log](#133-log)
      1.3.4. [Sidecar](#134-sidecar)
      1.3.5. [Volume](#135-volume)
   1.4. [04 Commands-types](#14-04-commands-types)
      1.4.1. [00 Index](#141-00-index)
      1.4.2. [01 CRUD Node](#142-01-crud-node)
      1.4.3. [DocumentationUpdate](#143-documentationupdate)
      1.4.4. [RegistryUpdate](#144-registryupdate)
2. [04 Templates](#2-04-templates)
   2.1. [00 Index](#21-00-index)
   2.2. [Skeleton-Node](#22-skeleton-node)

---

## 1. 02 Components

### 1.1. Index
*Источник: [[Index.documentation]] (строки 1-112)*

# Описание спецификации AWN V5

Ты работаешь в хранилище Obsidian. Баланс в том что и как видит человек и тем что и как видит видит ИИ (2 взаимопроникающих слоя)

> **Роль этого файла:** здесь описаны, принципы, команды, прозрачность. 
## Где мы работаем

Vault Obsidian — workspace агента. Не выходить за его пределы без явного разрешения пользователя.
## Метафора: клетки, узлы, синергия

Нода — как клетка организма: самодостаточный атом знания с понятной ролью. Цифровая экосистема строится из таких узлов — агент помнит и фиксирует происходящее в нужном файле, правит релевантные заметки, ориентируется где что лежит.

**Жёстких деревьев каталогов нет** — допустимы любые папки и файлы в vault. Важны не шаблоны папок, а договорённости и явные ноды.

Взаимодействие человека и ассистента — **синергия 1+1**: ясность агенту, опора и рост пользователю, без превращения памяти в кучу разрозненных дублей.
## Принципы содержимого

Ориентиры для нод и заметок — не законы, но помогают не превратить vault в свалку.
### Главные правила

**1. Не дублируй внутри одного файла.**

Одна мысль — один раз. Не повторяй одно и то же разными словами в разных местах одной ноды.

**2. Не дублируй между файлами — давай ссылку.**

Если факт уже есть в другой ноде — сошлись на неё, не копируй текст. Два одинаковых куска в разных файлах расходятся со временем и создают противоречия.

**3. Не вводи иерархию папок (областей/спейсов) там, где её нет в реальности или она избыточна.**

Папка оправдана, если внутри несколько связанных файлов с общим контекстом. Одиночная нода не требует своей папки — она живёт там, где логично.

**4. Принцип безопасной маршрутизации.**

Если не удаётся однозначно определить область/домен/папку для скрипта, текста, коллекции или документа (фото) — задавай уточняющий вопрос пользователю до создания/перемещения. Запрещено "угадывать" целевую папку при неоднозначности. "Неоднозначность" = есть 2+ равновероятных места, нет явных ключей в имени/контенте, или правило маршрутизации конфликтует.

### Таблица принципов

| Принцип            | Суть                                                                                                                    |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| **DRY**            | Один факт — один раз. Не дублировать текст между файлами — лучше ссылка на источник                                     |
| **DDD**            | Domain-Driven Design — домен и язык. Каждая область vault говорит на своём языке, ноды отражают реальные понятия домена |
| **KISS**           | Нода должна быть понятна с первого прочтения                                                                            |
| **YAGNI**          | Не создавать ноду «на будущее» — только когда реально нужна                                                             |
| **SRP** (из SOLID) | Одна нода — одна договорённость. Смешивать несвязанные темы в одном файле — плохо                                       |
| **APO**            | Atomicity / One Purpose — атом знания с понятной ролью                                                                  |
| **BDUF**           | Не строить вечную спецификацию заранее — держать договор в нодах, обновлять по мере роста                               |
| **Occam's Razor**  | Проще — вероятнее. Не плодить ноды и поля без реальной нужды                                                            |
                    
Любой элемент системы вида *.node.md* это нода
Нода может имет память
Суфиксы подсвечиваю в системе назначение (роль) файла

## О нодах и терминология

**AWN v3** — протокол человек ↔ агент в общем Vault.

- **Scope:** один Obsidian Vault.
- **Boundary:** агент не выходит за Vault без разрешения.
- **Модель:** Vault = OS для агента. Файловая структура = машинный API.
### Нода (Node)

Базовая единица. `*.md`-файл из таблицы типов (§3). Нода — файл с договорённостью: правило, профиль, навык, контекст.

**Инварианты:**

- 1 нода = 1 атомарная идея = 1 место.
- Тип ноды задаёт роль и поведение (§3).
- Поля фронтматтера зависят от типа.
- `.md`-файлы вне таблицы §3 — **не ноды**.

Нода это более менее статичный элемент - это логика
Память это динамически наполняемое редактируемое содержимое

Суффикс - это часть в названии папки или файла которая подсказыает назначение ее использования (роль)

## Слои контекста

Архитектурные слои по функции. Не путать с глубиной в дереве (§3). 
### Слой 1. Space. Общий контекст и правила области

- Контейнер. Задаёт границы и правила для всего, что внутри.
- Может содержать вложенные Space (группировка областей).
- Файл: `_index.node.md` внутри `space-*/`.
- Пример: `awn-spaces/space-finance/_index.node.md`.
### Слой 2. Part (части). Самостоятельные объекты

- Атомарные сущности внутри Space.
- Описание, правила, инструкции — стабильное содержимое.
- Файл: `*.node.md`.
- Пример: `user.node.md`, `git-push.node.md`.
### Слой 3. Memory — Память, следы, опыт, факты, воспоминания

Не истина, а сырьё для рефлексии.
Workflow обработки знаний: **inbox → note → page.**

- **Notes** — поток времени: чаты, сырые или оригинальные заметки. `.note.md` (обычно в `inbox/` или `notes/` соответствующей области). 
- **Pages** —  переработанные, устаявшиеся знания над которыми мы работаем . `pages/*.page.md` (аналогия страниц книг).
- **Rows** — структурированные записи (задачи, финансы и т.п.). `databases/<type>/*.row.md`.
- **Sidecars** — описание не-`.md` файлов (медиа, изображения, документы, бинарники). `*.sidecar.md`.
- **Volumes** (`*.node.volume.md`) — это личная память  ноды: НЕ грузится в контекст по умолчанию, а подгружается по запросу, когда агент работает именно с этой нодой.
### Слой 4. View — представления

- Проекции над слоями 1, 2, 3, 5: list, kanban, dashboard, mindmap.
- Не несёт собственного контента — только рендер.
- Файл: `views/<type>/<name>.view.md`.
### Слой 5. External — не-ноды

- Всё вне таблицы §3: оригиналы медиа, обычные `*.md` (Reference), скрипты, конфиги.
- Видны системе как сырьё/приложения, в граф нод не входят.

### 1.2. 02 Nodes-types

#### 1.2.1. Index
*Источник: [[02 Components/02 Nodes-types/Index.documentation.md]] (строки 1-13)*

# Система состоит из компонентов (нод и памяти)

Есть несколько типов нод (уровней слоя)

- нода-папка (описывает что это за папка и как работать с данными в ней)
- ноды-части - когда сложная нода разбивается на части
- память (воспоминание прикрепленное к ноде) - нодой не является
- все остальные файлы в системе

Одна нода - одна задача.
При создании ноды смотри шабло [Skeleton-Node.template](Skeleton-Node.template.md)

#### 1.2.2. 01 Folders

##### 1.2.2.1. 00 Index
*Источник: [[02 Components/02 Nodes-types/01 Folders/00 Index.documentation.md]] (строки 1-44)*

## Структура папок ноды

В компонентах типа "папка" допускается следующая структура (папки создаются по мере необходимости)

```
assets/ содержит вложения к записям ноды (по умолчанию раскладывается по папкам текущей даты YYYY-MM-DD/)
content/ содержит записи ноды с внешней памятью
scripts/ содержит разработанные скрипты (bash, python, другие)
parts/ содержит ноды-части
inbox/ папка по умолчанию для входящих данной тематики (сырые заметки)
```

Свойство AWN-CORE-CHILDREN-ALLOW - описыает разрешено ли создание дочерних нод 

Папки создаются по мере необходимости. Корневое рабочее дерево для всех областей — **`awn-spaces/`** (не засорять корень Vault; префикс `awn` отделяет фреймворк от стороннего кода в workspace).


### Папки внутри Space (по необходимости)

| Папка / файл                                                   | Назначение                                                                        |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `assets/`                                                      | Вложения, медиа (к ним — Sidecar `*.sidecar.md` рядом или по соглашению области). |
| `inbox/`                                                       | Сырые Note: `*.note.md`.                                                          |
| `notes/`                                                       | Note вне inbox (если нужен отдельный поток).                                      |
| `pages/`                                                       | Page: `*.page.md`.                                                                |
| `databases/<type>/`                                            | Row: `*.row.md`; `<type>` = имя подпапки (`default`, `calendar`, …).              |
| `views/<type>/`                                                | View: `*.view.md`.                                                                |
| `parts/` или `nodes/`                                          | Группировка Part `*.node.md` (опционально).                                       |
| `cron/`                                                        | Part-ноды автоматизации.                                                          |
| `scripts/`                                                     | Вспомогательные скрипты (External или не ноды).                                   |
| `templates/`                                                   | Заготовки.                                                                        |
| `_index.node.volume.md`                                        | Volume якоря Space (рядом с `_index.node.md`).                                    |
| `<name>.node.md`, `<name>.node.volume.md`, `<name>.resources/` | Part и спутники.                                                                  |
| `config.md`                                                    | Настройки области; может агрегироваться в глобальный конфиг (отдельное правило).  |

### Вариант `content/`

Если хочешь сгруппировать потоки памяти:

- `content/notes/` — эквивалент части `notes/` / `inbox/` по соглашению области.
- `content/<тип>/` — произвольный подтип контента.

Явно зафиксируй в `_index.node.md` области, какой вариант используется (**плоские папки** vs **`content/`**).

##### 1.2.2.2. 01 Workspace-vault
*Источник: [[01 Workspace-vault.documentation]] (строки 1-13)*

Файл вида "README.md" в корне хранилища - это нода описывающая зачем существует проект, что здесь лежит. описание проекта (корневая заметка). Предлагается создание всегда если ее нет! ошибка - что за проект о чем он

[SETTINGS]

key.value = какой-то ключ настройка
выносится в settings - в AGETNS.Settings.php - при компилязции пишется папка откуда пришла настройка

[/SETTINGS]

[CONFIG]
Аналогично
[/CONFIG]

##### 1.2.2.3. 02 Space
*Источник: [[02 Components/02 Nodes-types/01 Folders/02 Space.documentation.md]] (строки 1-8)*

---
AWN-CORE-TYPE: Folder
AWN-CORE-CHILDREN-ALLOW: "1"
AWN-CORE-ROLE: COMPONENT/SPACE
---
Файл вида "Название папки.space/Index.md" это нода оректиратор папки (область). Описывается зчем папка существует, что здесь лежит, зачем эта папка существует.

##### 1.2.2.4. 03 Cluster
*Источник: [[02 Components/02 Nodes-types/01 Folders/03 Cluster.documentation.md]] (строки 1-8)*

---
AWN-CORE-TYPE: Folder
AWN-CORE-CHILDREN-ALLOW: "1"
AWN-CORE-ROLE: COMPONENT/CLUSTER
---
Файл вида "Название папки.cluster/Index.md" это нода папки для группировки однотипных нод. 

##### 1.2.2.5. 04 Book
*Источник: [[02 Components/02 Nodes-types/01 Folders/04 Book.documentation.md]] (строки 1-6)*

---
AWN-CORE-TYPE: Folder
AWN-CORE-CHILDREN-ALLOW: "1"
AWN-CORE-ROLE: COMPONENT/BOOK
---
Файл вида "Название папки.book/Index.md" это нода содержащяя заметки напоминающая книгу. Описывает как работать с ней как сохранять. Подходит для базы знаний по определенной тематике.

##### 1.2.2.6. 05 Collection
*Источник: [[02 Components/02 Nodes-types/01 Folders/05 Collection.documentation.md]] (строки 1-6)*

---
AWN-CORE-TYPE: Folder
AWN-CORE-CHILDREN-ALLOW: "1"
AWN-CORE-ROLE: COMPONENT/COLLECTION
---
Файл вида "Название папки.collection/Index.md" это нода коллекция однотипных данных. Описывает как работать с ней как сохранять. Это аналог базы данных в папке. Подходит для однотипных данных - задачи - дневник - история финансов - логи и так далее.

##### 1.2.2.7. 06 Intro ИЛИ REPO
*Источник: [[zzz 06 Intro ИЛИ REPO.documentation]] (строки 1-11)*

---
AWN-CORE-TYPE: Folder
AWN-CORE-CHILDREN-ALLOW: "0"
AWN-CORE-ROLE: COMPONENT/REPO
---
Файл вида "Название папки.repo/Index.md" или "Название папки/Index.md" это нода-справочник (репозиторий или интро). 

Просто справочная информация о папке, о том, что здесь находится. Составляется оглавление того, что есть в папке.

Внутрь этой папки система автоматически ничего не создает и вглубь не идет. Папка конечна в контексте генерации структуры ИИ.

### 1.3. 03 Memories-types

#### 1.3.1. 00 Index
*Источник: [[02 Components/03 Memories-types/00 Index.documentation.md]] (пустой файл)*

#### 1.3.2. Content

##### 1.3.2.1. Inner
*Источник: [[02 Components/03 Memories-types/Content/Inner.documentation.md]] (строки 1-3)*

Внутренняя памят ноды - пишется в файл *.node.content.md* рядом с нодой
Yaml-свойства не используются

##### 1.3.2.2. Outer
*Источник: [[02 Components/03 Memories-types/Content/Outer.documentation.md]] (строки 1-2)*

Внешняя память ноды - пишется в файлы в папке *content/*
Базовый шаблон (схема) [[Memory.schema]] - имеет свой набор собвенных полей

#### 1.3.3. Log
*Источник: [[02 Components/03 Memories-types/Log.documentation.md]] (строки 1-5)*

---
AWN-SKELETON: Базовые свойства наследуются с шаблона SKELETON.template
AWN-ROLE-TYPE: MEMORY/LOG
---

#### 1.3.4. Sidecar
*Источник: [[02 Components/03 Memories-types/Sidecar.documentation.md]] (строки 1-2)*

Метоописание для файла (изображение документ другое) - обычно в папке Assets/
Создается по запросу. По умолчаию используется шаблон [[00 AWN-Framework.vault/04 Templates/Sidecar.template]]

#### 1.3.5. Volume
*Источник: [[02 Components/03 Memories-types/Volume.documentation.md]] (строки 1-5)*

*.node.volume.md

Создается рядом с нодой. Для агента - может записываться история взаимодействия - что как обсуждалось (некоторая внутренняя память для агента при работе и использовании ноды). Можно записывать то как рассуждал агент. Аналол томов в Docker. Не является источником правды и договоренностей.

Yaml-свойства не используются

### 1.4. 04 Commands-types

#### 1.4.1. 00 Index
*Источник: [[02 Components/04 Commands-types/00 Index.documentation.md]] (строки 1-1)*

Команды - это то что запрашивает пользователь и то что подсказыает агенту как с этим правильно работать

#### 1.4.2. 01 CRUD Node
*Источник: [[02 Components/04 Commands-types/01 CRUD Node.documentation.md]] (строки 1-6)*

Базовые CRUD операции при работе с нодами. Остальные детальные правила и назначения уже определяются в самой ноде

1) Спроси пользователя о чем будет нода
2) Если папка существует - создать в ней соответсвующий файл
3) Определи тип ноды
4) Предложи создать соответстующий раздел и файл ноды
5) Удаление производи с разрешения пользователя

#### 1.4.3. DocumentationUpdate
*Источник: [[DocumentationUpdate]] (строки 1-13)*

# Обновление документаций

Команда - обнови документацию

1) Найди все файлы *.documentation.md*
2) Приоритет отдавай AWN-фреймворку в начале выходного файла
3) Нумерацию параграфов задавай на основе древовидного содержимого и влженности папок и файлов
4) Собери (контатенция) содержимое всех файлов в виде оглавления книги 
5) К каждому файлу делай ссылку на источинк (start, end) что бы было понятно откуда пришла глава
6) Выходной файл сделай в Хранилище/<Папка AWN-Freamwork/05 Required/Documentation.combined.md
7) Ссылки на файлы заменяй на ссылку параграфа сгенерированного документа если она есть

Нумерацию параграфов и вложенность делаем согласно древовидной структуре расположнения каталогов. Документация подружается кжадый раз при старте агента через файл AGENTS.md

#### 1.4.4. RegistryUpdate
*Источник: [[RegistryUpdate]] (строки 1-10)*

# Обновление реестра нод

Все `*.node.md` регистрируются в `awn.registry.md`.

**Протокол синхронизации реестра:**

1. Найти все `*.node.md` по всему хранилищу (от корня)
2. Сверить с реестром — добавить новые, обновить изменённые, удалить неактуальные
3. Проверить что все пути относительные от корня проекта
4. При смысловом изменении YAML-шапки ноды (приоритет, триггеры, статус и т.д.) — обновить соответствующую строку

## 2. 04 Templates

### 2.1. 00 Index
*Источник: [[04 Templates/00 Index.documentation.md]] (строки 1-16)*

Канонические поля используют префикс `AWN-` — это отделяет системный контракт от пользовательских полей. Пользовательские поля (теги, заметки, метаданные проекта) пишутся без `AWN-`.

**Порядок ключей:** сначала все `AWN-*` поля, затем пользовательские. Состав полей может отличаться в зависимости от типа ноды.

## Контракт фронтматтера, формат шапки

В начале каждого файла-ноды и памяти ноды — блок YAML между `---`.

### Обязательные поля (все ноды)

### Что без фронтматтера

- **External** — произвольные файлы, не ноды.
- **`SKILL.md`** и прочие артефакты скилла — по правилам скилла, не по этому контракту, если иное не зафиксировано отдельно.

### 2.2. Skeleton-Node
*Источник: [[Skeleton-Node.documentation]] (строки 1-91)*

## Документация по полям нод

###  Свойство `AWN-STATUS` — жизненный цикл ноды

Статус — главный рубильник загрузки. **Агент читает ноду только если `AWN-STATUS: active`** — независимо от `AWN-LOAD` и триггеров.

| Статус | Влияние на загрузку | Когда использовать |
|--------|--------------------|--------------------|
| `active` | ✅ Загружается по правилам `AWN-LOAD` | Нода готова и работает |
| `disabled` | 🚫 Не загружается | Нода готова, но выключена — заглушка, временно не нужна |
| `draft` | 🚫 Не загружается | Нода в процессе написания — ещё не готова к работе |
| `archive` | 🚫 Не загружается | Нода устарела — файл сохраняется для истории |
###  **Свойство `AWN-PRIORITY` — приоритет загрузки нод (ориентир)**

| Диапазон | Смысл                                 |
| -------- | ------------------------------------- |
| 01–10    | Критичные: идентичность, безопасность |
| 11–30    | Важные: сильно влияют на контекст     |
| 31–70    | Рабочие: типовые темы и навыки        |
| 71–100   | Второстепенные: редкие, черновики     |
###  Свойство `AWN-MEMORY` — память ноды

Поле декларирует где хранится накопленная память по этой ноде:

| Значение   | Смысл                                                           |
| ---------- | --------------------------------------------------------------- |
| `none`     | Нода не ведёт память — просто правило или навык                 |
| `internal` | Журнал/факты хранятся в файле `*.node.content.md` рядом с нодой |
| `external` | Память в отдельном файле `Content/*.md`                         |
| `hybrid`   | И внутри ноды, и снаружи                                        |

Поле `AWN-MEMORY` — **справочная метка**, не исполняется автоматикой: не влияет на загрузку нод и реестр. Фактическое поведение задают текст ноды и явные инструкции в ней. По умолчанию для новых нод — `none`.

Файлы без `*.mode.md` — не ноды, в реестр не попадают. 
### Свойство `AWN-LOAD` — задаёт способ загрузки ноды

Свойство `AWN-TRIGGERS` — задаёт условие срабатывания ноды.

| `AWN-LOAD`       | Когда читать                                                                                                                                                                                                                                                      |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `start`-ноды     | Читается один раз при старте полной сессии — остаётся в контексте до конца сессии.                                                                                                                                                                                |
| `on_demand`-ноды | Читать если запрос **явно** совпадает с триггером: хотя бы один токен из `AWN-TRIGGERS` после приведения к нижнему регистру, с учётом очевидных опечаток и морфологических форм. Расширять по ассоциации — нельзя. ри нескольких совпадениях — по `AWN-PRIORITY`. |
### Прозрачность в ответах

В начале каждого ответа — одна строка:

| Ситуация                            | Строка                                                    |
| ----------------------------------- | --------------------------------------------------------- |
| Использована одна или несколько нод | `Контекст: использована нода <путь>` (пути через запятую) |
| Ноды не использовались              | `Контекст: ответ без ноды`                                |
| Пользователь попросил «без нод»     | `Контекст: ноды проигнорированы, кроме start`             |
**Важно:** в строку контекста подставлять только реальные `*.node.md` — путь относительно корня проекта. Системные файлы `AGENTS.md`, `awn.*.md`, нодами не являются — их указывать нельзя.

**Режим «Без нод»:** если пользователь явно пишет «без нод» — пропустить все `on_demand`-ноды на этот ответ. Start-ноды остаются.

| Поле             | Тип | Значение                                                                              |
| ---------------- | --- | ------------------------------------------------------------------------------------- |
| `AWN-ID`         | str | Стабильный идентификатор ноды. Не менять после создания. Формат: ULID или UUID v4/v7. |
| `AWN-TYPE`       | str | `Space`, `Part`, `Note`, `Page`, `Row`, `Sidecar`, `View`.                            |
| `AWN-STATUS`     | str | Жизненный цикл: `draft` \| `active` \| `archive` \| `disabled`.                       |

| Поле             | Тип | Назначение |
| ---------------- | --- | ---------- |
| `AWN-TITLE`      | str | Человекочитаемое имя, если не очевидно из имени файла. |
| `AWN-PARENT`     | str | `AWN-ID` родительской ноды. |
| `AWN-CATEGORY`   | str | Slug категории домена (если вводишь таксономию). |
| `AWN-UPDATED`    | str | ISO 8601 дата последнего смыслового обновления. |


```yaml
---
AWN-TYPE: "NODE/SOLO"
AWN-TITLE: Короткое имя
AWN-DESC: "Зачем эта нода — 1-2 строки"
AWN-LOAD: start | on_demand
AWN-PRIORITY: 01..100        # меньше = важнее
AWN-TRIGGERS: [слово, другое слово]
AWN-STATUS: active | disabled | draft | archive
AWN-CATEGORY: general          # slug категории; general — по умолчанию; system — системные ноды агента
AWN-MEMORY: none | internal | external | hybrid
AWN-AUTOMATIZATION: false    # true если нода запускается по расписанию
AWN-CRON: "0 9 * * *"        # расписание (только если AWN-AUTOMATIZATION: true)
AWN-VERSION: 1.0.0           # major — крупные изменения, minor — расширения, patch — правки
AWN-CREATED: YYYY-MM-DD
AWN-UPDATED: YYYY-MM-DD
# пользовательские поля — после AWN-блока:
TAGS: [тег1, тег2]
OTHER-KEY: example
---
```