## §1. Терминология

**AWN v3** — протокол человек ↔ агент в общем Vault.

- **Scope:** один Obsidian Vault.
- **Boundary:** агент не выходит за Vault без разрешения.
- **Модель:** Vault = OS для агента. Файловая структура = машинный API.
### Нода (Node)

Базовая единица. `*.md`-файл из таблицы типов (§3).

**Инварианты:**

- 1 нода = 1 атомарная идея = 1 место.
- Тип ноды задаёт роль и поведение (§3).
- Поля фронтматтера зависят от типа.
- `.md`-файлы вне таблицы §3 — **не ноды**.

## §2. Слои контекста

Архитектурные слои по функции. Не путать с глубиной в дереве (§3). 
### Слой 1. Clusters, Spaces. Общий контекст и правила области

- Контейнер. Задаёт границы и правила для всего, что внутри.
- Файл: `_index.node.md` внутри `space-*/` или `cluster-*/`.
- Пример: `awn-spaces/space-finance/_index.node.md`.
### Слой 2. Part (части). Самостоятельные объекты

- Атомарные сущности внутри Space.
- Описание, правила, инструкции — стабильное содержимое.
- Файл: `*.node.md`.
- Пример: `user.node.md`, `git-push.node.md`.
### Слой 3. Memory — Память, следы, опыт, факты, воспоминания

Не истина, а сырьё для рефлексии.
Workflow обработки знаний: **inbox → note → page.**

- **Notes** — поток времени: чаты, сырые или оригинальные заметки. `.note.md` (обычно в `inbox/` или `notes/` соответствующей области). 
- **Pages** —  переработанные, устаявшиеся знания над которыми мы работаем . `pages/*.page.md` (аналогия страниц книг).
- **Rows** — структурированные записи (задачи, финансы и т.п.). `databases/<type>/*.row.md`.
- **Sidecars** — описание не-`.md` файлов (медиа, изображения, документы, бинарники). `*.sidecar.md`.
- **Volumes** (`*.node.volume.md`) — это личная память  ноды: НЕ грузится в контекст по умолчанию, а подгружается по запросу, когда агент работает именно с этой нодой.
### Слой 4. View — представления

- Проекции над слоями 1, 2, 3, 5: list, kanban, dashboard, mindmap.
- Не несёт собственного контента — только рендер.
- Файл: `views/<type>/<name>.view.md`.
### Слой 5. External — не-ноды

- Всё вне таблицы §3: оригиналы медиа, обычные `*.md` (Reference), скрипты, конфиги.
- Видны системе как сырьё/приложения, в граф нод не входят.

## §3. Типы нод

Паттерн имени файла: `<name>.<type>.md`. Суффикс перед `.md` = тип ноды.
Агент по суффиксу однозначно определяет тип, не открывая файл.

| Слой        | Тип     | Файл             | Где живёт                  | Описание                                                   | Родитель                 |
| ----------- | ------- | ---------------- | -------------------------- | ---------------------------------------------------------- | ------------------------ |
| 0. Vault    | —       | —                | корень                     | Корень хранилища                                           | — (корень)               |
| 1. Cluster  | Cluster | `_index.node.md` | `cluster-*/`               | Группа близких пространств                                 | Vault, Cluster           |
| 1. Space    | Space   | `_index.node.md` | `space-*/`                 | Самостоятельная область знаний                             | Vault, Cluster, Space    |
| 2. Part     | Part    | `*.node.md`      | внутри Space               | Атомарная самостоятельная сущность                         | Space                    |
| 3. Memory   | Note    | `*.note.md`      | `inbox/` или `notes/`      | Сырая входящая заметка (поток времени)                     | Space                    |
| 3. Memory   | Page    | `*.page.md`      | `pages/`                   | Переработанное знание (страница книги)                     | Space                    |
| 3. Memory   | Row     | `*.row.md`       | `databases/<type>/`        | Строка таблицы базы данных                                 | Space                    |
| 3. Memory   | Sidecar | `*.sidecar.md`   | рядом с не-md файлом       | Описание не-md файла (медиа, документ, бинарь)             | Space (спутник external) |
| 3. Memory   | Volume  | `*.volume.md`    | рядом с родительской нодой | Приватная память ноды (грузится по запросу)                | Space, Part (спутник)    |
| 4. View     | View    | `*.view.md`      | `views/<type>/`            | Проекция над слоями 1–3, 5 (list/kanban/dashboard/mindmap) | Space                    |
| 5. External | —       | любые            | в любой области            | Не-ноды: медиа, бинари, скрипты, конфиги, `*.md` без типа  | Space                    |

**Правила:**

- Cluster и Space различаются префиксом папки (`cluster-*/` vs `space-*/`), файл-якорь у обоих один — `_index.node.md`.
- Volume применим только к Space и Part: `<имя>.node.volume.md` или `_index.node.volume.md`.
- Sidecar именуется по полному имени родителя: `photo.jpg.sidecar.md`, `lecture.mp4.sidecar.md`.
- External — всё, что не имеет распознаваемого типа в суффиксе. В граф нод не входит.
- Множественное число (`pages/`, `notes/`, `rows/`) — это **папки**. Сам тип ноды всегда в **единственном числе**.
- Любая вложенность вне колонки «Родитель» — нарушение контракта.

**Обратная карта (что-в-чём, выводится из «Родителя»):**

- **Vault** содержит: Cluster, Space, External.
- **Cluster** содержит: Cluster, Space.
- **Space** содержит: всё, кроме Vault — Cluster, Space, Part, Memory (Note/Page/Row/Sidecar/Volume), View, External.
- **Part** содержит: свой Volume и свою `<name>.resources/`.
- **Все остальные** (Note, Page, Row, Sidecar, Volume, View, External) — листы, нод не содержат.




---
- или §4 — контракт фронтматтера (`awn-id`, `awn-type`, `awn-status`),
- - §4 — структура папок ноды (как у тебя в `TODO.ADD.v3.md` было),