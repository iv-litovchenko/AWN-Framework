---
AWN-TYPE: "NODE/SOLO"
AWN-TITLE: Ежедневная проверка состояния системы
AWN-DESC: "По расписанию или по запросу: собрать снимок состояния окружения и записать его во внешнюю память (*.memory.md)."
AWN-LOAD: on_demand
AWN-PRIORITY: 30
AWN-TRIGGERS:
  - состояние системы
  - проверка состояния системы
  - system state
  - ежедневный отчёт
AWN-AUTOMATIZATION: true
AWN-CRON: "30 8 * * *"
AWN-STATUS: active
AWN-CATEGORY: system
AWN-VERSION: 1.0.3
AWN-CREATED: 2026-04-19
AWN-UPDATED: 2026-05-08
AWN-MEMORY: external
---

# Ежедневная проверка состояния системы

> [!info] О файле
> Нода с расписанием (8:30 каждый день) и ручным вызовом по триггерам. Результат каждого прогона — не отдельный «отчёт .md», а запись **`*.memory.md`** по контракту из `MemoryFiles.node.md`.

## Куда пишем память

- Каталог (от **корня проекта**): `awn-assistant-ai/Cron/Memory/`
- Имя файла: **`YYYY-MM-DD.memory.md`** (один файл на дату прогона).
- В шапке памяти: `AWN-TYPE: "RECORD/MEMORY"`, `AWN-OWNER: awn-assistant-ai/Cron/DailyHealthCheck.node.md`, `AWN-TITLE` — например «Состояние системы — YYYY-MM-DD».

## Что фиксировать в теле записи

1. **Доступность сервисов** — ключевые endpoints
2. **Место на диске** — свободное пространство
3. **Логи ошибок** — недавние ошибки
4. **Производительность** — время ответа

## Пример структуры тела (после шапки памяти)

```markdown
## Сервисы
- …

## Диск
- …

## Предупреждения
- …

## Метрики
- …
```

## Ручной запуск

Триггеры: «состояние системы», «проверка состояния системы», «system state», «ежедневный отчёт».
