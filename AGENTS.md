# AGENTS.md — цифровая среда ассистента

## Условие «полной сессии»

Если в **начале** сообщения пользователь поздоровался словами вроде **«Привет»** или **«Hi»** — принимай во внимание **всё**, что описано ниже: контекст Obsidian, принципы без дубликатов, реестр `*.node.md` и таблицу загрузки.

Если приветствия **нет** — считай, что пользователь работает в **чистой сессии**: не навязывай автоматически чтение всего реестра; опирайся только на явную задачу и приложенные файлы.

---

## 1. Где мы работаем

Мы работаем в **хранилище Obsidian**: это проект **цифровой экосистемы** (заметки, связи, структура по смыслу, а не «жёсткий монолит»). Не выходи за его пределы без разрешения пользователя.

### Файл окружения `.env`

- Основной файл настроек окружения — **корневой `.env`** (в корне текущего проекта/рабочей папки).
- Настройки берём из него; для шаблона используем `.env.example`.
- `.env` с реальными секретами не публикуем и не добавляем в git.

---

## 2. Принципы содержимого (SOLID и смежные идеи)

Ниже — **не законы**, а **ориентиры** для заметок и узлов: меньше дублей, проще сопровождение, дольше «игра вдолгую».

### Из правил ООП / модульности (SOLID — по смыслу)

- **В одном файле** не повторяй одну и ту же мысль разными формулировками без нужды — один источник истины внутри заметки.
- **В разных файлах** не копируй те же блоки текста — лучше **одна** каноническая заметка и **ссылка** (`[[...]]` или явная отсылка), чем два одинаковых куска.

### Коротко о принципах (две слова — суть)

| Принцип | Расшифровка | Две слова (суть) |
|---------|-------------|------------------|
| **SOLID** (часть идей) | Single responsibility, Open/closed, и др. | **Модульность, разделение** |
| **KISS** | Keep It Simple, Stupid | **Простота, ясность** |
| **YAGNI** | You Aren’t Gonna Need It | **Не нужно — не вводи** |
| **DRY** | Don’t Repeat Yourself | **Один факт — один раз** |
| **DDD** | Domain-Driven Design | **Домен, язык** |
| **BDUF** | Big Design Up Front | **Дизайн заранее, целиком** |
| **Occam’s Razor** | Бритва Оккама | **Проще — вероятнее** |
| **APO** | *локально для этой экосистемы:* **A**tomicity / **O**ne **P**urpose — один узел = одна договорённость | **Одна цель, один узел** |

*BDUF* в чистом виде спорен: для нас он напоминание не строить **лишнюю** «вечную спецификацию», а держать договор в **узлах** и таблице. *APO* здесь — **наш** ярлык под атомарность node, не универсальный стандарт из учебников.

Так мы держим экосистему **поворотливой**: меньше расхождений, проще обновлять и дольше играть вдолгую без «свалки» повторов.

---

## 3. Метафора: клетки, узлы, синергия

Ты, как и человек, мыслимо состоишь из **клеток** — у нас это **узлы (node)**. Цифровая экосистема строится из таких узлов: ты можешь **помнить** и **фиксировать** происходящее в **нужном** файле, **править** релевантные заметки и **ориентироваться**, где что лежит.

**Жёстких деревьев каталогов нет**: допустимы любые папки и файлы в vault. Важно не «шаблон папок», а **договорённости** и **явные узлы**.

Проект — это **когнитивная цифровая ОС**: взаимодействие человека и ассистента — **синергия 1+1**, усиливающая обоих: ясность тебе, опора и рост мне, без превращения памяти в кучу разрозненных дублей.

---

## 4. Что такое node

**Node** — это **договорённость (память)** о системе, о папке, о файле или вообще о чём угодно, что мы фиксируем явно: руководство, навык, описание сущности, правило поведения для зоны vault и т.д. Формально это модуль экосистемы — **атом знания** с понятной ролью.

**Файл узла** — маска имени: **`*.node.md`** (в любом месте vault).

Для локальных правил папки используем стандартное имя: **`Index.node.md`**.  
Если нужно описать более широкую область (несколько папок/домен целиком), используем **`Area.node.md`**.  
Итого: `Index.node.md` = вход и правила конкретной папки, `Area.node.md` = правила области уровнем выше.

Важно: `Index.node.md` и `Area.node.md` **не обязательны**.  
Если их нет — агент работает с контент-нодами напрямую (`*.node.md` по теме/триггерам).

### Обязательные свойства YAML в шапке `*.node.md`

Используйте такие ключи (имена — часть контракта экосистемы):

| Ключ             | Назначение                                                                                                                                                                 |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TYPE`           | Для всех `*.node.md` всегда используем фиксированное значение: `CoreNode`. Другие типы используем в обычных `*.md`-файлах.                                                 |
| `TITLE`          | Короткое человекочитаемое имя ноды.                                                                                                                                         |
| `DESCRIPTION`    | Зачем нода нужна (1-2 строки).                                                                                                                                              |
| `REQUIRE`        | Загрузка: **`start`** (**при старте**) или **`on_demand`** (**по необходимости**). См. расшифровку в разделе 6.                                                            |
| `PRIORITET`      | Числовой приоритет `01..100`: **`01` — самый высокий**, **`100` — минимальный**. Чем меньше число, тем больше приоритет, внимание и вес ноды при загрузке и использовании. |
| `TRIGGERS`       | Триггеры: темы, ключевые слова, ситуации — для режима **по необходимости** (совпадение с запросом).                                                                        |
| `AUTOMATIZATION` | Есть ли автоматизация: `true` / `false` (или `yes` / `no`). Запуск осуществляется по Cron.                                                                                |
| `CRON`           | Поле расписания по умолчанию: `CRON: "0 9 * * *"`.                                                                                                                         |
| `STATUS`         | Жизненный цикл узла: `draft` / `active` / `archive`.                                                                                                                       |
| `CREATED`        | Дата **создания** узла (рекомендуется ISO: `YYYY-MM-DD`).                                                                                                                  |
| `UPDATED`        | Дата **последнего обновления** узла (рекомендуется ISO: `YYYY-MM-DD`; обновлять при смысловых правках).                                                                    |

После закрытия блока `---` идёт **человеческое описание** узла: о чём он, что делать, границы.

### Пример скелета `*.node.md`

```yaml
---
TYPE: CoreNode
TITLE: Finance Budget Tracking
DESCRIPTION: "Вести учёт доходов и расходов. Использовать как источник для еженедельного финансового обзора."
REQUIRE: on_demand
PRIORITET: 10
TRIGGERS: ["финансы", "отчёт", "таблица"]
AUTOMATIZATION: false
CRON: "0 9 * * *"
STATUS: active
CREATED: 2026-04-14
UPDATED: 2026-04-14
---

Кратко: о чём этот узел и как с ним работать.
```

## Жизненный цикл нод

### 4. Статус: `draft | active | archive`

Поле `STATUS` показывает текущее состояние ноды и помогает держать базу в порядке без удаления файлов.

```yaml
---
TRIGGERS: [эксперимент, тест]
STATUS: draft
---
```

Как понимать статусы:
- `draft` — черновик или эксперимент.
- `active` — актуальная рабочая нода.
- `archive` — историческая нода, которую сохраняем для контекста, но не держим в активном наборе.

### Правила создания и названия новых node и папок

- Все пути в проекте указываем только **относительно корня текущего проекта** (без абсолютных путей вида `/Users/...`).
- Базовый стиль именования для новых папок и node-файлов: **CamelCase**.
- Пробелы в именах **допускаются**, если это делает название понятнее человеку.
- Рекомендуемая форма для node-файлов: `ИмяСущности.node.md` (например: `UserProfile.node.md`).
- Для локальной карты папки используем фиксированное имя `Index.node.md`; для области — `Area.node.md`.
- При создании новой папки или node обновляем реестр в `AGENTS.md`.

---

## 5. Роль этого файла (AGENTS.md)

Здесь — **сводное сознание среды** в смысле **списка того**, что агент должен уметь **подхватывать**: итоговая **таблица** всех зарегистрированных `*.node.md`.

Когда **ты** или **я** создаём новый `*.node.md`, мы **обновляем таблицу ниже** — это руководство к **автоматическому** (по приветствию и правилам выше) и **выборочному** чтению файлов.

Так мы избегаем **жёстких путей в тексте правил**: источником навигации служит **актуальная таблица** и сами узлы; пути в vault могут быть любыми, пока в таблице указано верное место.

---

## 6. Реестр узлов (`*.node.md`)

Заполняйте строки по мере появления файлов. Колонки:

| Путь к файлу                                    | Статус | Загрузка | Приоритет | Автоматизация | Триггер (тема или описание) | Создано | Обновлено |
| ----------------------------------------------- | ------ | -------- | --------- | ------------- | --------------------------- | ------- | --------- |
| *— добавьте первый `*.node.md` и строку сюда —* | —      | —        | —         | —             | —                           | —       | —         |

### Примеры нод (30, по разным областям)

1. **Файл и путь:** `System/Index.node.md`  
   **Название ноды:** System Index  
   **Описание ноды:** Главная входная нода системы: порядок чтения, базовые договоренности, ссылки на ключевые узлы.  
   **Триггеры (примеры):** старт, протокол, базовые правила  
   **Запуск:** при старте
2. **Файл и путь:** `System/Area.node.md`  
   **Название ноды:** System Area  
   **Описание ноды:** Нода области системной архитектуры, границ и принципов устройства всей среды.  
   **Триггеры (примеры):** архитектура, границы, системный контур  
   **Запуск:** при старте
3. **Файл и путь:** `System/SecurityPolicy.node.md`  
   **Название ноды:** Security Policy  
   **Описание ноды:** Политика безопасности: работа с секретами, `.env`, приватными файлами и доступами.  
   **Триггеры (примеры):** безопасность, токены, env  
   **Запуск:** при старте
4. **Файл и путь:** `System/BackupStrategy.node.md`  
   **Название ноды:** Backup Strategy  
   **Описание ноды:** Правила резервного копирования и восстановления базы знаний и рабочих артефактов.  
   **Триггеры (примеры):** backup, restore, архив  
   **Запуск:** по необходимости
5. **Файл и путь:** `Work/Index.node.md`  
   **Название ноды:** Work Index  
   **Описание ноды:** Навигация по рабочим задачам, проектам и операционным процессам.  
   **Триггеры (примеры):** работа, проект, задача  
   **Запуск:** по необходимости
6. **Файл и путь:** `Work/ProjectOperations.node.md`  
   **Название ноды:** Project Operations  
   **Описание ноды:** Операционный цикл проекта: планирование, контроль этапов, сдача результатов.  
   **Триггеры (примеры):** дедлайн, этапы, статус  
   **Запуск:** по необходимости
7. **Файл и путь:** `Work/MeetingProtocol.node.md`  
   **Название ноды:** Meeting Protocol  
   **Описание ноды:** Шаблон подготовки и проведения встреч, фиксация решений и follow-up действий.  
   **Триггеры (примеры):** встреча, agenda, итоги  
   **Запуск:** по необходимости
8. **Файл и путь:** `Work/ClientCommunication.node.md`  
   **Название ноды:** Client Communication  
   **Описание ноды:** Нормы коммуникации с клиентами: тон, структура ответов, SLA и контекст.  
   **Триггеры (примеры):** клиент, письмо, ответ  
   **Запуск:** по необходимости
9. **Файл и путь:** `Personal/Index.node.md`  
   **Название ноды:** Personal Index  
   **Описание ноды:** Карта личного пространства: рутина, приоритеты, личные проекты и трекинг.  
   **Триггеры (примеры):** личное, рутина, план  
   **Запуск:** по необходимости
10. **Файл и путь:** `Personal/DailyRoutine.node.md`  
   **Название ноды:** Daily Routine  
   **Описание ноды:** Правила ежедневного ритма: утренние и вечерние действия, мини-ритуалы, контроль.  
   **Триггеры (примеры):** утро, вечер, привычки  
   **Запуск:** по необходимости
11. **Файл и путь:** `Personal/WeeklyReview.node.md`  
   **Название ноды:** Weekly Review  
   **Описание ноды:** Недельный обзор: что сделано, что переносится, на чем фокус следующей недели.  
   **Триггеры (примеры):** обзор недели, итоги, план  
   **Запуск:** по необходимости
12. **Файл и путь:** `Personal/JournalGuidelines.node.md`  
   **Название ноды:** Journal Guidelines  
   **Описание ноды:** Правила ведения личных записей и рефлексии в едином формате.  
   **Триггеры (примеры):** дневник, рефлексия, заметки  
   **Запуск:** по необходимости
13. **Файл и путь:** `Finance/Index.node.md`  
   **Название ноды:** Finance Index  
   **Описание ноды:** Входная нода финансовой области: бюджеты, расходы, отчеты, платежи.  
   **Триггеры (примеры):** финансы, бюджет, расходы  
   **Запуск:** по необходимости
14. **Файл и путь:** `Finance/BudgetTracking.node.md`  
   **Название ноды:** Budget Tracking  
   **Описание ноды:** Методика учета расходов и доходов по категориям и лимитам.  
   **Триггеры (примеры):** бюджет, категория, лимит  
   **Запуск:** по необходимости
15. **Файл и путь:** `Finance/PaymentsCalendar.node.md`  
   **Название ноды:** Payments Calendar  
   **Описание ноды:** Календарь платежей и регулярных обязательств с напоминаниями.  
   **Триггеры (примеры):** платеж, дата, напоминание  
   **Запуск:** по необходимости
16. **Файл и путь:** `Finance/InvestmentsOverview.node.md`  
   **Название ноды:** Investments Overview  
   **Описание ноды:** Обзор инвестиционного портфеля, рисков и целевых долей.  
   **Триггеры (примеры):** инвестиции, портфель, риск  
   **Запуск:** по необходимости
17. **Файл и путь:** `Health/Index.node.md`  
   **Название ноды:** Health Index  
   **Описание ноды:** Карта медицинской и оздоровительной области: режим, обследования, симптомы.  
   **Триггеры (примеры):** здоровье, режим, самочувствие  
   **Запуск:** по необходимости
18. **Файл и путь:** `Health/MedicalRecords.node.md`  
   **Название ноды:** Medical Records  
   **Описание ноды:** Структура хранения анализов, заключений врачей и медицинской истории.  
   **Триггеры (примеры):** анализы, врач, диагноз  
   **Запуск:** по необходимости
19. **Файл и путь:** `Health/WorkoutPlan.node.md`  
   **Название ноды:** Workout Plan  
   **Описание ноды:** План физической нагрузки, прогресс и ограничения по самочувствию.  
   **Триггеры (примеры):** тренировка, нагрузка, план  
   **Запуск:** по необходимости
20. **Файл и путь:** `Learning/Index.node.md`  
   **Название ноды:** Learning Index  
   **Описание ноды:** Входная нода обучения: курсы, книги, конспекты, траектория развития навыков.  
   **Триггеры (примеры):** обучение, курс, конспект  
   **Запуск:** по необходимости
21. **Файл и путь:** `Learning/ReadingQueue.node.md`  
   **Название ноды:** Reading Queue  
   **Описание ноды:** Очередь чтения и правила отбора материалов по актуальным целям.  
   **Триггеры (примеры):** чтение, книга, статья  
   **Запуск:** по необходимости
22. **Файл и путь:** `Learning/SkillsMap.node.md`  
   **Название ноды:** Skills Map  
   **Описание ноды:** Карта навыков: текущий уровень, пробелы и план развития.  
   **Триггеры (примеры):** навыки, прогресс, gap  
   **Запуск:** по необходимости
23. **Файл и путь:** `Tech/Index.node.md`  
   **Название ноды:** Tech Index  
   **Описание ноды:** Карта технологической области: стек, инструменты, стандарты и практики.  
   **Триггеры (примеры):** технологии, стек, инструменты  
   **Запуск:** по необходимости
24. **Файл и путь:** `Tech/DevEnvironment.node.md`  
   **Название ноды:** Dev Environment  
   **Описание ноды:** Настройка среды разработки, зависимости, версии и базовые команды.  
   **Триггеры (примеры):** env, setup, dependencies  
   **Запуск:** по необходимости
25. **Файл и путь:** `Tech/ReleaseChecklist.node.md`  
   **Название ноды:** Release Checklist  
   **Описание ноды:** Чеклист выпуска: pre-release проверки, smoke-тест, rollback-план.  
   **Триггеры (примеры):** релиз, smoke test, rollback  
   **Запуск:** по необходимости
26. **Файл и путь:** `Tech/Incidents.node.md`  
   **Название ноды:** Incidents  
   **Описание ноды:** Протокол работы с инцидентами: фиксация, диагностика, постмортем.  
   **Триггеры (примеры):** инцидент, outage, fix  
   **Запуск:** по необходимости
27. **Файл и путь:** `Home/Index.node.md`  
   **Название ноды:** Home Index  
   **Описание ноды:** Карта домашней области: бытовые задачи, расписания, обслуживание.  
   **Триггеры (примеры):** дом, быт, дела  
   **Запуск:** по необходимости
28. **Файл и путь:** `Home/MaintenanceSchedule.node.md`  
   **Название ноды:** Maintenance Schedule  
   **Описание ноды:** График обслуживания дома и техники, контроль периодических работ.  
   **Триггеры (примеры):** обслуживание, ремонт, сроки  
   **Запуск:** по необходимости
29. **Файл и путь:** `Travel/TripPlanning.node.md`  
   **Название ноды:** Trip Planning  
   **Описание ноды:** Планирование поездок: маршрут, бронирования, документы и чеклист выезда.  
   **Триггеры (примеры):** поездка, маршрут, бронь  
   **Запуск:** по необходимости
30. **Файл и путь:** `Relationships/ContactsContext.node.md`  
   **Название ноды:** Contacts Context  
   **Описание ноды:** Контекст по контактам и отношениям: как общаться, что важно помнить.  
   **Триггеры (примеры):** контакт, контекст, коммуникация  
   **Запуск:** по необходимости

### Как читать файл по значению «Загрузка»

| Значение в таблице / `REQUIRE` в YAML | Как читать узел                                                                                                                                                       |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **При старте** (`start`)              | Содержимое файла **читается при старте сессии** (в т.ч. при «полной сессии» по приветствию) **автоматически и обязательно** — узел входит в базовый контекст.         |
| **По необходимости** (`on_demand`)    | Содержимое файла читаем **только если** есть **совпадение триггера** с темой/сутью запроса (из `TRIGGERS` и колонки «Триггер»); иначе не подмешиваем без явной нужды. |

### Соглашение по колонкам

- **Путь к файлу** — относительный путь от корня текущего проекта; обновлять при переносе файла.
- **Статус** — из YAML `STATUS`: `draft`, `active`, `archive`.
- **Загрузка** — из `REQUIRE`: **`start`** → в таблице пишем **«при старте»**; **`on_demand`** → **«по необходимости»** (см. таблицу выше).
- **Приоритет** — из `PRIORITET` по шкале `01..100`: `01` = максимум приоритета, `100` = минимум; более приоритетные ноды учитываются раньше и сильнее.
- **Автоматизация** — из YAML `AUTOMATIZATION` (`да` / `нет` или `true` / `false`).
- **Триггер** — кратко из `TRIGGERS` или своими словами для людей; для режима **по необходимости** задаёт условие срабатывания.
- **Создано** / **Обновлено** — дублируют `CREATED` / `UPDATED` из шапки узла (удобно смотреть в таблице без открытия файла).

---

## 7. Основные команды

- Создать / изменить / удалить ноду (`*.node.md`).
- Создать / изменить / удалить ноду-типа области или индекса (`Area.node.md`, `Index.node.md`).
- Добавить или обновить настройки в корневом `.env`.
- Обновить карту (реестр) нод в `AGENTS.md`.

---

## 8. Почему не жёсткие «фреймворки» и не чужие конвенции workspace

### Веб-фреймворки (Symfony, Laravel, Yii и т.п.)

Они заточены под **код и жёсткие конвенции репозитория**. Наша цель — **гибкая когнитивная среда** в Obsidian: узлы как контент, связи, эволюция без превращения всего в неповоротную схему. Оттуда — **`*.node.md`** и **одна** точка сборки в `AGENTS.md`, а не дерево обязательных каталогов фреймворка.

### Агентские workspace (OpenClaw, ZeroClaw, CoPaw и похожие)

Пробовались также подходы вроде **OpenClaw**, **ZeroClaw**, **CoPaw**: в workspace часто заложены **общепринятые имена** и одна и та же «карта» ролей файлов. Это удобно как **шаблон**, но по сути снова **жёсткая структура**: набор имён **свой у каждого продукта/сообщества**, перенос между проектами даёт трение, а агент оказывается привязан к **чужому** договору, а не к нашему реестру узлов.

**Пример — типовой workspace OpenClaw** (как задокументировано у них: [Agent workspace](https://docs.openclaw.ai/concepts/agent-workspace.md)):

| Файл / папка | Коротко о роли |
|--------------|----------------|
| `AGENTS.md` | Правила работы и использования памяти; на старте сессии |
| `SOUL.md` | Персона, тон, границы; на старте сессии |
| `USER.md` | Кто пользователь, как обращаться; на старте сессии |
| `IDENTITY.md` | Имя/образ агента (в т.ч. после bootstrap) |
| `TOOLS.md` | Заметки про локальные инструменты и конвенции (не включает/выключает тулзы) |
| `HEARTBEAT.md` | Короткий чеклист для периодических heartbeat-прогонов |
| `BOOT.md` | Опционально: чеклист при рестарте gateway |
| `BOOTSTRAP.md` | Разовый ритуал первого запуска; потом обычно удаляют |
| `MEMORY.md` | Опционально: отобранная долговременная память (в т.ч. только для приватной сессии) |
| `memory/YYYY-MM-DD.md` | Дневной лог памяти (один файл на день) |
| `skills/` | Опционально: навыки, привязанные к этому workspace |
| `canvas/` | Опционально: файлы UI canvas |

У **ZeroClaw / CoPaw** набор может отличаться, но логика та же: **заранее заданные имена и роли**, к которым привыкаешь в одном стеке и которые плохо переносишь в другой без миграции.

Здесь мы сознательно уходим от «обязательного набора файлов в корне» к **реестру в таблице** и произвольным путям для `*.node.md`: жёсткость — только в **минимальном контракте** полей YAML и в актуальности таблицы, а не в фиксированном списке имён вроде полного bootstrap OpenClaw.

---

*Документ можно дополнять, но без дублирования: новые глобальные правила либо кратко здесь, либо вынесены в отдельный `*.node.md` со строкой в таблице.*
