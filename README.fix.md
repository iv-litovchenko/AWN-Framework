# AWN-Framework v2

## Что это

**AWN-Framework** (Agent-Workspace-Node) — это протокол организации цифрового хранилища, в котором человек и LLM-агент работают с одними и теми же файлами.

Основная идея: vault — это не просто набор заметок, а **когнитивная ОС**: структурированная среда, где знания, правила поведения агента, данные и память живут в одном месте и понятны как человеку, так и любому агенту.

Три ключевых элемента системы:

- **Ноды (`*.node.md`)** — атомы знания и договорённостей. Каждая нода описывает одну конкретную вещь: правило, навык, домен, схему. Агент читает их по триггерам или при старте.
- **Реестр (`AGENTS.NODES.md`)** — карта всех нод: что загружать при старте, что по запросу, какие приоритеты.
- **Домены (`Domains/`)** — пространства знаний пользователя (английский язык, финансы, проекты и т.д.). Каждый домен — папка со своим `Index.node.md`-дашбордом, заметками, файлами и памятью.

Основной просмотрщик для человека — **Obsidian**. Агент работает с теми же файлами через любую совместимую среду:

- **IDE-агенты:** Cursor, Antigravity и аналоги
- **LLM-агенты и фреймворки:** Claude (Cowork), Codex, Hermes Agent, Agent Zero, OpenClaw, QwenClaw и другие
- **API:** любой LLM с доступом к файловой системе vault

Vault не привязан к конкретному инструменту — точка входа `AGENTS.md` (режим сессии), полный протокол `AGENTS.FULL.md`; любой агент, следуя им, понимает среду и может работать с хранилищем.

## Главный принцип

**Не где это лежит, а как найти.**

База знаний строится не вокруг жёсткой иерархии папок, а вокруг договорённостей: ноды (`*.node.md`), реестр (`AGENTS.NODES.md`), конвенции именования. Любая структура папок — вторична.

## Цель

База знаний должна быть **переносимой от агента к агенту**: любой LLM-агент, прочитав `AGENTS.md`, при полной сессии — `AGENTS.FULL.md` и `AGENTS.NODES.md`, понимает где что лежит, как себя вести и как работать с доменами пользователя. Человек читает те же файлы в Obsidian.

---

## Открытые вопросы

Вопросы зафиксированы для проработки — ответы будут появляться по мере развития системы.

### 1. Взаимодействие с внешними агентами (оркестрация)

Принцип работы Агент-Сервер и Агент-Клиент.

### 2. Переносимость скриптов и областей

Возможность переносить доменные области, ноды и скрипты от одного агента к другому.

### 3. Привязка файлов к заметкам vs самостоятельные файлы

Два подхода к Assets: файл всегда привязан к заметке через sidecar `*.metadata.md` — или может лежать как «склад» без описания? Нужно ли запрещать файлы без sidecar в `Assets/`?

### 4. Структура доменов: подпапки или плоско?

Внутри домена `Notes/` — всё плоско или допустимы подпапки по типу (`Notes/Grammar/`, `Notes/Vocabulary/`)? Когда подпапки оправданы, а когда достаточно поля `type` в frontmatter?

### 5. Синхронизация Vault между устройствами

Как синхронизировать vault между MacBook, сервером и телефоном? Варианты: Яндекс.Диск, Google Drive, iCloud, Obsidian Sync, Syncthing, Git. Что работает надёжно с бинарными файлами в `Assets/`?

### 6. Расшаривание и доступ по HTTPS

Когда vault лежит на сервере — как дать ссылку на просмотр или скачивание документа по HTTPS? Варианты: Obsidian Publish, nginx + static, self-hosted Quartz/MkDocs.

### 7. Obsidian-скилл для агента

Будет ли помогать Obsidian-скилл агенту, чтобы искать и управлять заметками в Obsidian через API или плагин (Dataview, Local REST API)? Будет ли работать поиск "по смыслу", а не только glob, grep, read.

### 8. Index.md — оглавление всех файлов

Нужен ли глобальный `Index.md` в корне vault — список всех файлов с описанием (по аналогии с Andrej Karpathy)? Как его поддерживать актуальным — вручную или автоматически? Где это уместно создавать еще?

### 9. Access.md — управление доступом (RBAC / ACL)

Нужен ли файл `Access.node.md` с политиками доступа к заметкам: `public`, `private`, `allow: [список]`? Как агент должен учитывать эти ограничения при ответах и расшаривании?

### 10. Data/ — структурированные данные и схемы

Нужна ли отдельная папка `Data/` (или `Base/`) для хранения структурированных данных — схем, записей типа «БД», которые агент ведёт автоматически (задачи, контакты, события и т.п.)? Открытые вопросы: чем это отличается от `Domains/`, как просматривать и редактировать такие записи в Obsidian (Dataview, Obsidian Bases, CSV), когда оправдано выделять отдельную папку, а когда достаточно добавить домен в `Domains/`?

### 11. Area.node.md vs Index.node.md — нужны ли оба типа?

Оба типа живут на уровне папки, имеют одинаковые YAML-поля и по смыслу описывают «вход в зону». Различие сейчас — концептуальное: `Area` — контекст для агента (широкая зона), `Index` — дашборд для пользователя (конкретная папка с Dataview-запросами). Вопрос: достаточно ли одного типа (`NODE/INDEX`) и позволить содержимому самому определять роль файла? Или различие `NODE/AREA` / `NODE/INDEX` несёт ценность и должно остаться?

Варианты решения:

- **A — оставить как есть** (`NODE/AREA` + `NODE/INDEX`). Различие концептуально полезно: `Area` задаёт контекст/тон зоны для агента, `Index` — навигационный дашборд с Dataview для пользователя. Минус: два типа с одинаковыми полями, граница размыта.
- **B — оставить только `Index.node.md`** (`NODE/INDEX`). Одно имя, один тип — содержимое само показывает, широкая это зона или конкретный дашборд. Меньше концепций, проще объяснять новому агенту.
- **C — переименовать для ясности**: `Zone.node.md` (широкая область, `NODE/ZONE`) и `Index.node.md` (конкретный дашборд, `NODE/INDEX`). Смысл сохраняется, имена говорят сами за себя.

Определиться точно с тем что есть
Node Solo
Node Index
Node Area
Node Domain (домен - по идее это может быть любая папка в том числе папка документации, папка ассистента, папка тестов с заранее определенной структурой Assets/ Notes/ Memory/ Scripts/ Nodes/ )

### 12. Repositories/ — внешние репозитории и зависимости vault, нужен ли ".repo" для папок? 

Vault знает о **знаниях** пользователя через домены, но ничего не знает о его **кодовых проектах**. Идея: добавить папку `Repositories/` и файл `awn.composer.json` для описания внешних репозиториев, с которыми работает пользователь локально.

**Структура:**

- `awn.composer.json` — машиночитаемый манифест всех репозиториев: `id`, локальный `path`, `git`-адрес, `stack`, `status`. Единственный источник истины для определений.
- `Repositories/Index.node.md` — человекочитаемый вход: таблица репозиториев, команды для агента.
- Опционально: отдельная нода на каждый крупный репозиторий с заметками, архитектурой, TODO.

**Связь с доменами (гибридный подход):**

- В `Index.node.md` домена добавляется поле `repositories: ["id"]` — только ссылка на ID, без дублирования деталей.
- Агент при работе в домене видит `repositories`, идёт в `awn.composer.json`, получает путь и стек.
- Принцип: домен объявляет **зависимость** (`require`), `awn.composer.json` объявляет **определение** — как `require` + `composer.json` в PHP.

**Пример `awn.composer.json`:**

```json
{
  "vault": "MyVault",
  "repositories": {
    "project-alpha": {
      "path": "~/Projects/alpha",
      "git": "https://github.com/user/alpha.git",
      "stack": "Next.js / Payload",
      "status": "active"
    },
    "project-beta": {
      "path": "~/Projects/beta",
      "git": "https://github.com/user/beta.git",
      "stack": "PHP 8.1 / WordPress",
      "status": "active"
    }
  }
}
```

**Открытые вопросы:**

1. Где лежит `awn.composer.json` — в корне vault или в `Repositories/`?
2. Нужен ли аналог `composer.lock` — фиксация коммита/версии репозитория?
3. Что агент умеет делать с этим файлом: только читать контекст или также проверять статус (`git status`, `health-check`)?
4. Как быть с репозиториями без домена — просто в `awn.composer.json` без ноды?

### 13. Многослойная память агента — архитектура поверх vault

AWN-Framework описывает структуру знаний внутри vault, но vault — только один из слоёв памяти агентской системы в целом. Концепция трёх слоёв:

- **Слой 1 — Vault (ноды + домены)** — курируемые знания, договорённости, правила. Человек редактирует вручную, агент читает по протоколу AWN. Главный источник истины.
- **Слой 2 — Verbatim-история (MemPalace / sqlite-vec / аналоги)** — полная сырая история переписок, семантический поиск. Используется когда нужно найти «что именно мы говорили 3 месяца назад». Внешний сервис, не часть vault.
- **Слой 3 — Temporal graph (Graphiti / Zep AI / аналоги)** — автоматически извлечённые сущности и связи с временны́ми метками. Позволяет задавать вопросы вида «как изменились наши правила за последние полгода?». Внешний сервис на базе Neo4j / FalkorDB.

**Ключевой принцип:** слои 2 и 3 — инфраструктурные компоненты агентской платформы (под капотом), они не описываются нодами в vault. Vault занимается только слоем 1.

**Открытый вопрос:** нужен ли в vault какой-то лёгкий «манифест» подключённых внешних слоёв памяти (аналог `awn.composer.json` для репозиториев)? Или это зона ответственности агентской платформы, а не vault-протокола?

----

## Остаточное TODO

- **Collections/** (нужна ли такая папка - аналог Domains/ но для списокв - коллекций ссылок - материалов - фильмов - ссылок - закладок - документов) - коллекция 1 - коллекция 2 - коллекция - 3 (и закономерный вопро нужна ли Documents коллекция?)
- **Scripts****/** - здесь размещаются все скрипты (например python) – которые сгенерировал агент и которые нужны для работы хранилища
- **Domain Externa**l (когда может жить в другой папке (например Documentation, Assitent.Ai - по сути это тоже домены)) - нужно ли это?
- **Tests/Setting** - протестировать каждую настройку AGENTS.SETTINGS.md
- **Вопрос иерархии папок** (например как вот в документации у меня есть область переписки чатов и они разложены по папкам - Grok ChatGPT CLaude и так далее - есть ли смысл это сохранять так?)
- Можно ли в обсидиан как то сделать открыть ссылку на папку на мак?
- Внедрить оперирование агентом файлами docer-compose.yml, .env - что бы он понимал что это есть и использовал эти файлы
- Также вопрос по поводу изменения config-а своей системы (пишет ли он туда изменения или нет)
- UPDATE.md (обновление системы с git-репозитория)

Да, в Obsidian можно сделать ссылку, которая открывает папку в Finder на Mac.
[Открыть папку Downloads](file:///Users/твоё_имя_пользователя/Downloads)
[Открыть Downloads](<file:///Users/macbook/Downloads>)

**Скрипты** 

Папка Scripts/
Папка Content/ - для любого другого контента не md, файлы
Docker-compose.yml (линукс VS docker)
.env

**Дашборд** 

- вкладка в WEB-интерфейсе кастомизация
- нужны ли какие-то настройки выводить на дашборд в нодах что бы она туда попадала и дашборд сам перестраивался - будет что-то вроде нового параметра)
- Мне понравилось то что в переписке Гермес дает отсылки к файлам - было бы круто иметь возможность их просматривать

**Переносимость и обновляемость**

Важно - в дальнейшем нужно будет придумать (разделить) - файлы на редактируемые и не редактируемые - иначе мы систему не сможем обновлять когда агенты пишут - системные файлы должны быть что-то вроде

/AssistentAi/Assistent.node.core.md  - или что то типа так (не изменная часть)
/AssistentAi/Assistent.node.md 

Так и фреймворк можно будет обновлять (но как это пока сопоставить все пока не понятно - и нужны ли манифесты)

## V3



Идея скомпилированной ноды:

- `Assistant.Ai/Assistant.core.node.md`
- `Assistant.Ai/Assistant.user.node.md`
- `Assistant.Ai/Assistant.node.md` (compiled)

И для GitPull:

- `Assistant.Ai/Nodes/GitPull.core.node.md`
- `Assistant.Ai/Nodes/GitPull.user.node.md`
- `Assistant.Ai/Nodes/GitPull.node.md` (compiled)

## Правило приоритета (самое важное)

- Core = канон и инварианты
- User = только разрешённые override-поля
- Итог: `effective = core + user_overrides`
- Если user конфликтует с “жёстким” правилом core → core побеждает, конфликт логируется/показывается

Nodes/

├── User.node.core.md          ← "Код" фреймворка
│   ├── AWN-Name: User
│   ├── AWN-Version: 2.1
│   ├── AWN-Protocol: ...      ← правила, триггеры, контракты
│   └── ⚠️ Обновляется ТОЛЬКО при апдейте фреймворка
│
├── User.node.memory.md        ← "Внутренняя память" ноды
│   ├── Контекст сессий
│   ├── Предпочтения пользователя
│   ├── Краткосрочные состояния
│   └️ ⚠️ Пишет ТОЛЬКО агент/пользователь
│
└── Memory/                    ← "Внешняя память" (холодное хранение)
    ├── 2025-12-10.memory.md
    ├──
    └── 📁 по датам / темам / доменам

1. Переименовать домены в Spaces  Я склоняюсь к тому что это должно называться все же spaces а не домены
	1.  Орбиты (Orbits)
	2. Потоки (Streams)
	3. Пространства (Spaces)
		Если называть «Доменом» только верхний уровень, то он начинает работать как **контейнер** или «Макро-контекст». Такой подход превращает плоский список в **иерархическую структуру**, и это логически очень верно. В системной архитектуре это часто называют **Namespace** (Пространство имен). Это помогает агентам не путаться: когда агент работает внутри домена «Работа», он понимает, что «Проект 1» и «Проект 2» связаны общими ресурсами, финансами или твоим временем. **Твоя идея с `Домен/Проект` — самая рабочая.** Она позволяет тебе масштабироваться бесконечно. Завтра у тебя появится домен `Home`, и туда уедут `Ремонт`, `Покупки` и `Сад`. Система останется стройной.
		Группа 
			**Кластер** (Cluster)
			**Сектор** (Sector)
			**Хаб** (Hub)
2. Убрать Areas (сделать загрузку описания в реестр нод) без триггеров У нас останется только.  нода одиночка 
Нода (область)
Спейсы 
Категория спейсов 
3. Expel draw dashboards (дашборд рисунок)
4. В реестре нод в таблицах лучше сделать вторая строка это описание Ноды триггер - так будет читаться лучше
5. Идея как отличать служебные ноды от других - есть постыикс .awn.node.md ключ